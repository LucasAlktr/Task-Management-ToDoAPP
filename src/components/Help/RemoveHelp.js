import React from "react";

function RemoveHelp ()  {
  return (
    <>
      <h2>Removing Tasks.</h2>
      <div>
        <p>To delete a task, simply navigate to the task you want to remove, then look for the delete or trash icon associated with the task. Tap or click on it, and confirm the deletion if prompted. The task will then be permanently removed from your list.</p>
      </div>
    
    </>
  );
};

export default RemoveHelp;